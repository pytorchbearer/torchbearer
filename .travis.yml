language: python
python:
  - "3.6"
# command to install dependencies
install:
  - python setup.py develop
# command to run tests
script:
  - nosetests tests -v --with-coverage --cover-package=sconce
after_success:
  - bash < (curl -s https://codecov.io/bash)